apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronyd
spec:
  selector:
    matchLabels:
      app: chronyd
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: <Image>
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
